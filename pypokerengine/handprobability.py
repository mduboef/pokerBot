import engine.hand_evaluator
from engine.hand_evaluator import *

def testProbs(hand):
    print(hand)

def simulateHands(holeCards, commCards):
    return

if __name__ == '__main__':
    testProbs(['CQ', 'D5'])
    #handEval.HandEvaluator.__is_onepair(['C2', 'C3', 'D3'])
    test = engine.hand_evaluator.HandEvaluator()
    test.__is_onepair(['D6', 'C6'])